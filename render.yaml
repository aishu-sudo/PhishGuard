services:
  - type: web
    name: phishguard-api
    env: docker
    plan: free
    region: singapore
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: VT_AUTO_SUBMIT
        value: "true"
